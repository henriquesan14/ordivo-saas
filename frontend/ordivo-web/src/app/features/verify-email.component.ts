import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core';
import { ActivatedRoute } from '@angular/router';
import { Api, errorMessage } from '../core/api';

@Component({selector:'app-verify-email',changeDetection:ChangeDetectionStrategy.OnPush,template:`<main><section class="card"><a class="brand" href="/login"><span>O</span> Ordivo</a>@if(loading()){<div class="loader"></div><h1>Confirmando seu e-mail...</h1>}@else if(success()){<div class="check">✓</div><h1>E-mail confirmado</h1><p>Sua empresa está pronta. Entre com o e-mail e a senha usados no cadastro.</p><a class="primary" href="/login">Entrar no Ordivo</a>}@else{<div class="error">!</div><h1>Não foi possível confirmar</h1><p>{{message()}}</p><a class="secondary" href="/login">Voltar ao login</a>}</section></main>`,styles:[`:host{display:block}main{min-height:100vh;display:grid;place-items:center;padding:24px;background:#f4f6f1}.card{width:min(440px,100%);padding:38px;text-align:center}.brand{display:flex;align-items:center;justify-content:center;gap:11px;margin-bottom:40px;font-size:21px;font-weight:800}.brand span{display:grid;place-items:center;width:34px;height:34px;border-radius:10px;background:#173f39;color:#e6ff72;font-family:Georgia,serif;font-style:italic}.check,.error{display:grid;place-items:center;width:58px;height:58px;margin:auto;border-radius:50%;font-size:26px}.check{background:#e5f2ed;color:#286658}.error{background:#fff0ed;color:#a83c2d}h1{font-size:28px;margin:18px 0 10px}p{color:#74817f;line-height:1.6;font-size:13px}.primary,.secondary{display:inline-flex;margin-top:14px}.secondary{padding:11px 16px;border:1px solid #ccd7cf;border-radius:9px;font-weight:800}.loader{width:40px;height:40px;margin:auto;border:4px solid #e2e9e3;border-top-color:#173f39;border-radius:50%;animation:spin .8s linear infinite}@keyframes spin{to{transform:rotate(360deg)}}`]})
export class VerifyEmailComponent{private api=inject(Api);private route=inject(ActivatedRoute);loading=signal(true);success=signal(false);message=signal('O link é inválido ou expirou.');constructor(){const token=this.route.snapshot.queryParamMap.get('token');if(!token){this.loading.set(false);return}this.api.post<boolean>('/api/auth/verify-email',{token}).subscribe({next:()=>{this.success.set(true);this.loading.set(false)},error:error=>{this.message.set(errorMessage(error));this.loading.set(false)}})}}
